# Server Configuration
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Database (update with your PostgreSQL credentials)
#DATABASE_URL=postgresql+asyncpg://seedeep:seedeep123@localhost:5432/seedeep_db
DATABASE_URL=sqlite+aiosqlite:///./seedeep.db
# Model Configuration
CONFIDENCE_THRESHOLD=0.5
IOU_THRESHOLD=0.45
MAX_DETECTIONS=100
FORCE_CPU=false

# CORS (use "*" for all origins, or comma-separated list)
ALLOWED_ORIGINS=*

# Logging
LOG_LEVEL=INFO